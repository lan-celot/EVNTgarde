import React from "react";
import ReactDOM from "react-dom/client";
import "./RegistrationLogin"; // Import Tailwind styles
import App from "./RegistrationLogin";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);